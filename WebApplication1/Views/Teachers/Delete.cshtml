@model WebApplication1.Models.Teacher

@{
    ViewData["Title"] = "Удалить преподавателя";
}

<div class="container mt-4">
    <h2 class="mb-3 text-center text-danger">🗑️ Удаление преподавателя</h2>
    <hr />

    <div class="card shadow-sm border-danger mx-auto" style="max-width: 600px;">
        <div class="card-body">
            <h4 class="card-title text-center mb-3 text-danger">@Model.FullName</h4>
            <p class="text-center text-muted mb-4">
                Вы уверены, что хотите <strong>удалить</strong> этого преподавателя? Это действие необратимо.
            </p>

            <dl class="row small">
                <dt class="col-sm-4">Дата рождения</dt>
                <dd class="col-sm-8">@Model.Dob.ToString("dd.MM.yyyy")</dd>

                <dt class="col-sm-4">Пол</dt>
                <dd class="col-sm-8">@Model.Gender</dd>

                <dt class="col-sm-4">Образование</dt>
                <dd class="col-sm-8">@Model.Education</dd>

                <dt class="col-sm-4">Категория</dt>
                <dd class="col-sm-8">@Model.Category</dd>
            </dl>

            <form asp-action="Delete" method="post" class="mt-4 text-center">
                <input type="hidden" asp-for="TeacherId" />
                <button type="submit" class="btn btn-danger me-2">
                    ❌ Удалить
                </button>
                <a asp-action="Index" class="btn btn-secondary">
                    ↩ Отмена
                </a>
            </form>
        </div>
    </div>
</div>
